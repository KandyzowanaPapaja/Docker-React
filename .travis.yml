language: generic 
sudo: required
services:
  - docker

before_install:
  - docker build -t rafal712/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true rafal712/docker-react npm run test